version: "2"

linters:
  default: standard
  settings:
    staticcheck:
      checks:
        - all
    errcheck:
      check-type-assertions: true

formatters:
  enable:
    - gofmt
    - goimports
    - gofumpt

issues:
  max-issues-per-linter: 0
  max-same-issues: 0

