apiVersion: batch/v1
kind: Job
metadata:
  name: preview-gc
spec:
  template:
    spec:
      containers:
        - name: preview-gc
          resources:
            limits:
              memory: 256Mi
