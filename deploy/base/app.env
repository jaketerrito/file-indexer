# Base environment configuration, consumed by every S3-speaking workload via
# envFrom configMapRef app-config. Overlays override individual keys with
# their own app.env (configMapGenerator behavior: merge).
#
# Plaintext S3, for the local overlay's in-cluster MinIO. Production sets
# S3_SECURE=true in its own app.env.
S3_SECURE=false
